# This directory contains the SQLite database file (settings.db)
# The database file is created automatically when the application runs
# and is stored in the Data folder relative to where the application is running
